apiVersion: v1
kind: Secret
metadata:
  name: {{ template "keeper.fullname" . }}
  namespace: {{ .Release.Namespace }}
  labels:
    {{- include "keeper.labels" . | nindent 4 }}
type: Opaque
data:
  KEEPER_PRIVATE_KEY: {{ .Values.settings.keeperPrivateKey | b64enc | quote }}
