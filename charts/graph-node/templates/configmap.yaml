apiVersion: v1
kind: ConfigMap
metadata:
  name: {{ include "common.names.fullname" . }}-config
  labels:
    {{- include "common.labels.standard" . | nindent 4 }}
data:
  config.toml: |
    {{ print (tpl .Values.graph.configTemplate $) | nindent 4 }}
