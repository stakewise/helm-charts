{{- if .Values.prysm.enabled }}
{{- if .Values.prysm.p2pNodePort.enabled -}}
{{- range $i, $e := until (int .Values.global.replicaCount) }}
{{- $port := add $.Values.prysm.p2pNodePort.startAt $i -}}
{{- if hasKey $.Values.prysm.p2pNodePort.replicaToNodePort ($i | toString) -}}
  {{ $port = index $.Values.prysm.p2pNodePort.replicaToNodePort ($i | toString) }}
{{- end }}

---
apiVersion: v1
kind: Service
metadata:
  name: {{ include "common.names.fullname" $ }}-beacon-{{ $i }}
  labels:
    {{- include "common.labels.standard" $ | nindent 4 }}
    pod: "{{ include "common.names.fullname" $ }}-{{ $i }}"
    type: p2p
    client: beacon
  {{- with $.Values.prysm.p2pNodePort.annotations }}
  annotations:
    {{- toYaml . | nindent 4 }}
  {{- end }}
spec:
  type: {{ $.Values.prysm.p2pNodePort.type }}
  externalTrafficPolicy: Local
  ports:
    - name: p2p-tcp-beacon
      port: {{ include "prysm.p2pPort" $ }}
      protocol: TCP
      targetPort: p2p-tcp-beacon
      nodePort: {{ $port }}
  {{- if eq $.Values.prysm.p2pNodePort.type "NodePort" }}
    - name: p2p-udp-beacon
      port: {{ include "prysm.p2pPort" $ }}
      protocol: UDP
      targetPort: p2p-udp-beacon
      nodePort: {{ $port }}
  {{- end }}
  selector:
    {{- include "common.labels.matchLabels" $ | nindent 4 }}
    statefulset.kubernetes.io/pod-name: "{{ include "common.names.fullname" $ }}-{{ $i }}"

{{- end }}
{{- end }}
{{- end }}
