apiVersion: v1
kind: Service
metadata:
  name: {{ include "nimbus.fullname" . }}
  labels:
    {{- include "nimbus.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
{{- if .Values.svcHeadless }}
  clusterIP: None
{{- end }}
{{- if .Values.sessionAffinity.enabled }}
  sessionAffinity: ClientIP
  sessionAffinityConfig:
    clientIP:
      timeoutSeconds: {{ .Values.sessionAffinity.timeoutSeconds }}
{{- end }}
  ports:
  {{- if .Values.nimbus.discv5.enabled }}
    - port: {{ .Values.nimbus.discv5.tcpPort }}
      targetPort: p2p-tcp
      protocol: TCP
      name: p2p-tcp
    - port: {{ .Values.nimbus.discv5.udpPort }}
      targetPort: p2p-udp
      protocol: UDP
      name: p2p-udp
  {{- end }}
  {{- if .Values.nimbus.rpc.enabled }}
    - port: {{ .Values.nimbus.rpc.port }}
      targetPort: json-rpc
      protocol: TCP
      name: json-rpc
  {{- end }}
  {{- if .Values.nimbus.rest.enabled }}
    - port: {{ .Values.nimbus.rest.port }}
      targetPort: rest
      protocol: TCP
      name: rest
  {{- end }}
  {{- if .Values.metrics.enabled }}
    - port: {{ .Values.metrics.port }}
      targetPort: metrics
      protocol: TCP
      name: metrics
  {{- end }}
  selector:
    {{- include "nimbus.selectorLabels" . | nindent 4 }}
