{{- if and .Values.global.rbac.create .Values.beaconChainRpcEndpointsAutoDiscovery -}}
apiVersion: rbac.authorization.k8s.io/v1
kind: Role
metadata:
  name: {{ template "validators.fullname" . }}
  labels:
    {{- include "validators.labels" . | nindent 4 }}
rules:
{{- toYaml .Values.global.rbac.rules | nindent 0 }}
{{- end }}
