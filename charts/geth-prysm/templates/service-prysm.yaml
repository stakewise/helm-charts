---
apiVersion: v1
kind: Service
metadata:
  name: {{ include "common.names.fullname" . }}-prysm
  labels:
    {{- include "common.labels.standard" . | nindent 4 }}
{{- if and .Values.prysm.metrics.enabled .Values.prysm.metrics.svcAnnotations }}
  annotations:
    {{ toYaml .Values.prysm.metrics.svcAnnotations | nindent 4 | trim }}
{{- end }}
spec:
{{- if .Values.prysm.sessionAffinity.enabled }}
  sessionAffinity: ClientIP
  sessionAffinityConfig:
    clientIP:
      timeoutSeconds: {{ .Values.sessionAffinity.timeoutSeconds }}
{{- end }}
  type: ClusterIP
{{- if .Values.svcHeadless }}
  clusterIP: None
{{- end }}
  ports:
    - name: {{ .Values.prysm.rpc.portName }}
      port: {{ .Values.prysm.rpc.port }}
      targetPort: {{ .Values.prysm.rpc.portName }}
  {{- if .Values.prysm.http.enabled }}
    - name: http
      port: {{ .Values.prysm.http.port }}
      targetPort: http
  {{- end }}
  {{- if .Values.global.metrics.enabled }}
    - name: metrics
      port: {{ .Values.prysm.metrics.port }}
      targetPort: metrics
  {{- end }}
  selector:
    {{- include "common.labels.matchLabels" . | nindent 4 }}
