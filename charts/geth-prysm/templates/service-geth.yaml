{{- if or .Values.geth.http.enabled .Values.geth.ws.enabled }}
---
apiVersion: v1
kind: Service
metadata:
  name: {{ include "common.names.fullname" . }}-geth
  labels:
    {{- include "common.labels.standard" . | nindent 4 }}
spec:
{{- if .Values.geth.svcHeadless }}
  clusterIP: None
{{- end }}
{{- if .Values.geth.sessionAffinity.enabled }}
  sessionAffinity: ClientIP
  sessionAffinityConfig:
    clientIP:
      timeoutSeconds: {{ .Values.geth.sessionAffinity.timeoutSeconds }}
{{- end }}
  type: ClusterIP
  ports:
  {{- if .Values.global.JWTSecret }}
    - name: authrpc-geth
      port: {{ .Values.geth.authRpc.port }}
      targetPort: authrpc-geth
  {{- end }}
  {{- if .Values.geth.http.enabled }}
    - name: http-geth
      port: {{ .Values.geth.http.port }}
      targetPort: http-geth
  {{- end }}
  {{- if .Values.geth.ws.enabled }}
    - name: ws-geth
      port: {{ .Values.geth.ws.port }}
      targetPort: ws-geth
  {{- end }}
  {{- if .Values.global.metrics.enabled }}
    - name: metrics-geth
      port: {{ .Values.geth.metrics.port }}
      targetPort: metrics-geth
  {{- end }}
  selector:
    {{- include "common.labels.matchLabels" . | nindent 4 }}
{{- end }}
