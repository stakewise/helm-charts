{{- if and .Values.validator.enabled }}
apiVersion: v1
kind: ServiceAccount
metadata:
  name: {{ template "validator.fullname" . }}
  namespace: {{ .Release.Namespace }}
  labels:
    {{- include "validator.labels" . | nindent 4 }}
  annotations:
    "helm.sh/resource-policy": keep
{{- end }}
