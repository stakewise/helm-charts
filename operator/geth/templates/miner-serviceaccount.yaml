{{- if .Values.miner.enabled }}
apiVersion: v1
kind: ServiceAccount
metadata:
  name: {{ include "geth.fullname" . }}-miner
  labels:
    {{- include "geth.labels" . | nindent 4 }}
    component: "miner"
{{- end }}
