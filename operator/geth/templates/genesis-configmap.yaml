{{- if and .Values.genesis }}
apiVersion: v1
kind: ConfigMap
metadata:
  name: {{ template "genesis.fullname" . }}
  namespace: {{ .Release.Namespace | quote }}
  labels:
    {{- include "genesis.labels" . | nindent 4 }}
data:
  genesis: |
    {{ .Values.genesis | nindent 4 | trim }}
{{- end }}
