{{- if and .Values.miner.enabled .Values.genesis }}
apiVersion: v1
kind: ConfigMap
metadata:
  name: {{ include "geth.fullname" . }}-genesis
  labels:
    {{- include "geth.labels" . | nindent 4 }}
    component: genesis
data:
  genesis: |
    {{ .Values.genesis | nindent 4 | trim }}
{{- end }}
