{{- if .Values.node.enabled }}
apiVersion: v1
kind: ServiceAccount
metadata:
  name: {{ include "geth.fullname" . }}-node
  labels:
    {{- include "geth.labels" . | nindent 4 }}
    component: "node"
{{- end }}
