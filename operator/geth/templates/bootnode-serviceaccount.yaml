{{- if .Values.bootnode.enabled }}
apiVersion: v1
kind: ServiceAccount
metadata:
  name: {{ include "geth.fullname" . }}-bootnode
  labels:
    {{- include "geth.labels" . | nindent 4 }}
    component: "bootnode"
{{- end }}
