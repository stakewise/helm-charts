{{- if and .Values.lighthouse.beacon.enabled .Values.lighthouse.beacon.metrics.enabled .Values.lighthouse.beacon.metrics.prometheusRule.enabled }}
apiVersion: monitoring.coreos.com/v1
kind: PrometheusRule
metadata:
  name: {{ template "node-operator.fullname" . }}-lighthouse-beacon
  {{- if .Values.lighthouse.beacon.metrics.prometheusRule.namespace }}
  namespace: {{ .Values.lighthouse.beacon.metrics.prometheusRule.namespace }}
  {{- else }}
  namespace: {{ .Release.Namespace | quote }}
  {{- end }}
  labels:
    {{- include "node-operator.labels" . | nindent 4 }}
    {{- include "lighthouse-beacon.labels" . | nindent 4 }}
    {{- if .Values.lighthouse.beacon.metrics.prometheusRule.additionalLabels }}
    {{- toYaml .Values.lighthouse.beacon.metrics.prometheusRule.additionalLabels | nindent 4 }}
    {{- end }}
spec:
  {{- with .Values.lighthouse.beacon.metrics.prometheusRule.rules }}
  groups:
    - name: {{ template "node-operator.fullname" $ }}-lighthouse-beacon
      rules: {{- tpl (toYaml .) $ | nindent 8 }}
  {{- end }}
{{- end }}