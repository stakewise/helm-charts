{{- if and .Values.geth.enabled .Values.geth.verticalAutoscaler.enabled }}
apiVersion: autoscaling.k8s.io/v1beta2
kind: VerticalPodAutoscaler
metadata:
  name: {{ template "node-operator.fullname" . }}-geth
  labels:
    {{- include "node-operator.labels" . | nindent 4 }}
    {{- include "geth.labels" . | nindent 4 }}
spec:
  targetRef:
    apiVersion: "apps/v1"
    kind: StatefulSet
    name: {{ template "node-operator.fullname" . }}-geth
  updatePolicy:
    updateMode: {{ .Values.geth.verticalAutoscaler.updateMode | default "Off" | quote }}
  {{- if .Values.geth.verticalAutoscaler.containerPolicies }}
  resourcePolicy:
    containerPolicies:
      {{ tpl .Values.geth.verticalAutoscaler.containerPolicies . | nindent 6 | trim }}
  {{- end }}
{{- end }}
