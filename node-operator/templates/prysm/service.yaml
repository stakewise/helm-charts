{{- if and .Values.prysm.enabled .Values.prysm.beacon.enabled }}
apiVersion: v1
kind: Service
metadata:
  name: {{ template "node-operator.fullname" . }}-prysm-beacon
  labels:
    {{- include "node-operator.labels" . | nindent 4 }}
    {{- include "prysm-beacon.labels" . | nindent 4 }}
{{- if and .Values.prysm.beacon.metrics.enabled .Values.prysm.beacon.metrics.svcAnnotations }}
  annotations:
    {{ toYaml .Values.prysm.beacon.metrics.svcAnnotations | nindent 4 | trim }}
{{- end }}
spec:
  selector:
    {{- include "node-operator.selectorLabels" . | nindent 4 }}
    {{- include "prysm-beacon.labels" . | nindent 4 }}
{{- if .Values.prysm.beacon.sessionAffinity.enabled }}
  sessionAffinity: ClientIP
  sessionAffinityConfig:
    clientIP:
      timeoutSeconds: {{ .Values.prysm.beacon.sessionAffinity.timeoutSeconds }}
{{- end }}
  type: ClusterIP
{{- if .Values.prysm.beacon.svcHeadless }}
  clusterIP: None
{{- end }}
  ports:
    - name: grpc
      port: 4000
      targetPort: grpc
  {{- if .Values.prysm.beacon.metrics.enabled }}
    - name: metrics
      port: {{ .Values.prysm.beacon.metrics.port }}
      targetPort: metrics
  {{- end }}
{{- end }}
