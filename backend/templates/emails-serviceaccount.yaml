{{- if .Values.emailCronJobs.enabled }}
apiVersion: v1
kind: ServiceAccount
metadata:
  name: {{ template "emails.fullname" . }}
  labels:
    {{- include "backend.labels" . | nindent 4 }}
{{- end }}
