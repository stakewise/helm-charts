{{- if .Values.polling.deployments }}
apiVersion: v1
kind: ServiceAccount
metadata:
  name: {{ template "polling.fullname" . }}
  labels:
    {{- include "backend.labels" . | nindent 4 }}
    component: "polling"
{{- end }}
